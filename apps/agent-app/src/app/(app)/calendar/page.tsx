'use client';

import { AppBarCategory } from '@real-estate/ui/app-bar-category';

export default function CalendarPage() {
  return (
    <AppBarCategory
      title="Activités"
      onAdd={() => {}}
      onSearch={() => {}}
    />
  );
}
